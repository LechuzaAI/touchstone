from setuptools import setup

setup(name='touchstone',
      version='0.0.1',
      install_requires=['gym',
                        'torch',
                        'numpy',
                        'pytorch-lightning'])
